# Used only by the commented host-port mapping in compose.yml.
CHECKMATE_PORT=52345

# Public URL the browser reaches Checkmate on. Wrong value breaks the login flow.
CHECKMATE_CLIENT_HOST=http://localhost:52345

CHECKMATE_DB_NAME=checkmate
CHECKMATE_JWT_SECRET={{openssl_rand_32}}

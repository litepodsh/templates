# Used only by the commented host-port mapping in compose.yml.
FILEBROWSER_PORT=8080

# Path inside the container that File Browser serves. The filebrowser_srv volume
# is mounted here; swap it for another stack's volume to manage those files.
FILEBROWSER_ROOT=/srv

FILEBROWSER_DATABASE=/database/filebrowser.db

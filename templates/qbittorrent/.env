# User/group the container runs as — must own the named volumes' contents.
PUID=1000
PGID=1000
TZ=UTC

# Web UI port. Also used for the internal expose.
QBITTORRENT_WEBUI_PORT=8080

# BitTorrent peer port, published on the host for both TCP and UDP.
QBITTORRENT_TORRENTING_PORT=6881

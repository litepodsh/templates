# Security note

This is a community-maintained container, not an image published by the Tor Project. It is bound to `127.0.0.1` by default because its browser session must not be exposed without access control. Create a VNC password inside the running container before making it reachable from another host.

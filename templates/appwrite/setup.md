# Before deploying

Replace the two Appwrite secrets and the MongoDB password. This compact template provides Appwrite's API with MongoDB and Redis. Appwrite's complete upstream deployment also includes the Console, Traefik routing and specialized workers; use the official generated Compose setup when you need those features, because it requires Docker-socket access that this catalog intentionally does not use.

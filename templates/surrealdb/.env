# Used only by the commented host-port mapping in compose.yml.
SURREAL_PORT=8000

SURREAL_USER=root
SURREAL_PASS={{password_32}}
SURREAL_LOG_LEVEL=info

# Convenience defaults for clients / `surreal sql` — not read by the server.
SURREAL_NS=default
SURREAL_DB=default

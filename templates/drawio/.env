# Used only by the commented host-port mapping in compose.yml.
DRAWIO_PORT=8080

# Public hostname browsers use to reach this editor (no scheme).
DRAWIO_PUBLIC_HOST=localhost
DRAWIO_BASE_URL=http://localhost:8080
DRAWIO_CSP_HEADER=
DRAWIO_LETS_ENCRYPT_ENABLED=false

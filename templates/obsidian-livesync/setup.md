# Complete the LiveSync setup

After CouchDB starts, run the LiveSync project's CouchDB initialization procedure, then configure the **Self-hosted LiveSync** community plugin in Obsidian with the full CouchDB URL, username, password and a database name. Use HTTPS before connecting mobile clients over the Internet.
